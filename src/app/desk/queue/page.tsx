// Desk-accessible queue page — same content as /admin/queue but within desk layout
// This keeps the receptionist inside the desk layout (no cross-layout navigation)
export { default } from "@/app/admin/queue/page";
